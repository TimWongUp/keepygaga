# Antigravity CLI Hooks

`keepygaga install --yes --host antigravity` projects Context Bootstrap and Memory Route into `~/.gemini/config/hooks.json` under `shared-context-bootstrap`. Antigravity CLI is not Gemini CLI. Closeout is intentionally omitted because the available post-tool output is not a reliable model-visible reminder.
