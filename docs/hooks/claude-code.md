# Claude Code Hooks

`keepygaga install --yes --host claude-code` projects built-in Context Bootstrap, Memory Route, compact recovery, and Memory Closeout commands into `~/.claude/settings.json`. Commands use the installed `keepygaga` launcher and current absolute config path. Unrelated matchers and commands are preserved.
