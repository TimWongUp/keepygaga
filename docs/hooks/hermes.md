# Hermes Hooks

`keepygaga install --yes --host hermes` projects Context Bootstrap and Memory Route at `pre_llm_call`, and Memory Closeout at `pre_verify`, within `~/.hermes/config.yaml`. Round-trip YAML merging preserves unrelated keys, comments, ordering, and Hook entries.
